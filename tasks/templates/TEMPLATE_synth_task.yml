---
schema_version: 1
id: "" # optional unique task id (YYYYMMDD-<slug> recommended)
title: "" # concise summary
agent: "synth" # MUST be "synth"
priority: P1 # P0|P1|P2
due_date: "" # ISO 8601 (YYYY-MM-DD or ISO datetime)
assignee: "" # optional
source_files: [] # REQUIRED list of paths to research artifacts
deliver_to: [] # REQUIRED list of repo paths for outputs
pillar_map: [] # e.g., ["Negotiation","Job Search"]
dod:
  criteria: [] # acceptance checks specific to this task
guardrails: [] # domain, tone, privacy, etc
notes: "" # optional free-text
# UPGRADE
